server:
  port: 8989
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: file:/home/will/Documentos/cloud-server-config
management:
  security:
    enabled: false